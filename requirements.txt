# Core (min versions work on Python 3.8+; use Python 3.8+ if you see "no matching distribution")
torch>=1.13
numpy>=1.21
pandas>=1.3
scikit-learn>=1.0
pyarrow>=10.0  # for Parquet I/O

# API / HTTP
requests>=2.25

# Testing
pytest>=7.0

# Optional: async requests for faster data fetching
# aiohttp>=3.8
